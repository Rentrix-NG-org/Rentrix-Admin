import { Box } from "@mui/material";

const Topbar = () => {
  return (
    <Box
      sx={{
        height: "fit-content",
        padding: "20px",
        boxShadow: "0px 4px 4px 0px rgba(0, 0, 0, 0.05)",
      }}
    >
      Topbar
    </Box>
  );
};
export default Topbar;
